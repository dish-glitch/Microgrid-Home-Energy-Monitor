# Firmware

ESP32 firmware written in C++ using the Arduino framework.

## Dependencies (to be added)

- `EmonLib` — RMS sampling and power calculation
- `Adafruit SSD1306` — OLED display driver
- `ESPAsyncWebServer` — WiFi dashboard

## Build

Open in Arduino IDE or PlatformIO. Select board: `ESP32 Dev Module`.
